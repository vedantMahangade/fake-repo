console.log("Batch mint is deprecated — tickets mint on primary purchase.");
console.log("Use: node scripts/05-primary-sale.js [priceHbar]");
console.log("Or buy via the marketplace UI (POST /api/tokens/{tokenId}/buy).");
process.exit(1);
